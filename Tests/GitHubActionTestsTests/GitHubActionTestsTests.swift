import XCTest
@testable import GitHubActionTests

final class GitHubActionTestsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(GitHubActionTests().text, "Hello, World!")
    }
}
